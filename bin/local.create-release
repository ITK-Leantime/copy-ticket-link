#!/usr/bin/env bash
set -o errexit -o errtrace -o noclobber -o nounset -o pipefail

export plugin_name=CopyTicketLink
export plugin_repo_url=https://github.com/ITK-Leantime/copy-ticket-link/

buildRelease() {
  return
}

export -f buildRelease
